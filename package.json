{
  "name": "dust-dice-contract",
  "version": "0.4.4",
  "private": true,
  "description": "Dust Dice \u2014 a Yahtzee-style dice game on Midnight: the Compact contracts, their TypeScript mirrors and witnesses, the independent game verifier, and the protocol documentation. Local devnet / testnet only.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "contract",
    "api",
    "verifier"
  ],
  "//overrides": "@midnight-ntwrk/wallet-sdk 2.0.0-beta.2 depends on wallet-sdk-utilities EXACTLY 1.2.0, but its siblings (facade, dust-wallet, capabilities) import `Clock` from it, and `Clock` only exists from 1.2.1. Without this override every wallet import dies at load time with \"does not provide an export named 'Clock'\" \u2014 a link error that names no version and no culprit. Drop it once a beta ships with the dependency corrected.",
  "overrides": {
    "@midnight-ntwrk/wallet-sdk-utilities": "1.2.1"
  },
  "scripts": {
    "compact": "npm run compact -w contract",
    "build": "npm run build -w contract && npm run build -w api && npm run build -w verifier",
    "lint": "eslint .",
    "format": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "verify": "npm run verify -w verifier --"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.69.0"
  }
}
